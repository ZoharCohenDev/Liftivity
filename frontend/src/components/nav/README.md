# components/nav

Navigation components used by the `AppLayout`.

## Components

- `Sidebar` — left-side navigation with logo, nav items, and usage bar
- `TopBar` — top app bar with search, New Analysis button, notifications, and avatar
