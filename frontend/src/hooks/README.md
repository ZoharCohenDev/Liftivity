# hooks

Shared custom React hooks used across multiple pages or components.

## What to implement

- `useAnalysis.ts` — fetch and poll analysis status
- `useSites.ts` — fetch and manage the user's site list
- `useDebounce.ts` — debounce a value (useful for search inputs)
- `useLocalStorage.ts` — persist state to localStorage with a typed interface
