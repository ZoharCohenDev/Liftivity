# theme

MUI theme configuration and design token exports.

## Files

- `theme.ts` — `getTheme(mode)` factory that returns a full MUI theme for dark and light mode; also exports `SIDEBAR_COLORS` and `TOPBAR_COLORS` constants used by nav components
