# pages/Dashboard/components

Components used exclusively by `DashboardPage`. Do not import these from other pages.

- `ConversionChart` — area chart showing conversion lift trends
- `IssueBreakdownChart` — bar chart of issues grouped by category
- `RecentAnalysesTable` — paginated table of the user's recent site analyses with status and score
