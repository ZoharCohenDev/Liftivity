# utils

Shared pure helper functions used across the frontend.

## What to implement

- `formatters.ts` — number formatting (scores, percentages, large numbers), date formatting
- `scoring.ts` — score-to-color mapping, score-to-grade conversion
- `url.ts` — URL parsing, domain extraction, validation helpers
- `api.ts` — typed `fetch` wrapper with auth headers and error handling
