# layouts

Page layout wrappers that define the shell around route-level pages.

## Layouts

- `AppLayout` — authenticated app shell: `Sidebar` + `TopBar` + `<Outlet />` for page content
- `PublicLayout` — public-facing shell: `LandingNav` + `<Outlet />` + `LandingFooter`
