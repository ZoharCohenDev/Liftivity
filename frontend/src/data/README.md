# data

Static mock data used during development before the backend API is connected.

## Files

- `mockData.ts` — all mock data exports: sites, analyses, charts, user, pricing plans, etc.

When the backend is ready, replace context data fetching with real API calls and remove this folder's usage from components.
