# @liftivity/web

React + Vite frontend for the Liftivity platform.

## Dev

```bash
pnpm dev        # starts Vite dev server on http://localhost:5173
pnpm build      # production build → dist/
pnpm typecheck  # TypeScript check only
```
