# infra/db

Database migration and seed files.

| Folder | Purpose |
|--------|---------|
| `migrations/` | Ordered SQL migration scripts (`NNN_description.sql`) |
| `seeds/` | Dev seed data — never run in production |
