# infra

Infrastructure configuration for local development and production deployment.

| Folder | Purpose |
|---|---|
| `compose/` | Docker Compose files for orchestrating all services together |
| `db/` | Database migrations and seed data |
| `docker/` | Dockerfiles and build configuration |
