# packages/ui/src

Shared React component library.

Components promoted here are available to both `frontend` and any future consumer apps in the monorepo.

## What to implement

Move components here when they are needed by more than one app in the workspace.
