# packages/ui/src/components

Shared UI components available to all apps in the monorepo.

## What to implement

Promote components from `frontend/src/components/` here when they need to be consumed by more than one app. Keep the API identical to the original so imports are a drop-in replacement.
