# packages

Shared internal packages used across `frontend`, `backend`, and `worker` in the monorepo.

| Package | Purpose |
|---|---|
| `shared-types` | TypeScript types shared between frontend and backend (API contracts, domain models) |
| `ui` | Shared React component library (if components are promoted from frontend) |
| `config` | Shared ESLint and TypeScript base configurations |
