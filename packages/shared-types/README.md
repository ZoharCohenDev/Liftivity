# @liftivity/shared-types

TypeScript type definitions shared across all apps and packages in the monorepo.

## Usage

```ts
import type { User, Workout, Exercise } from "@liftivity/shared-types";
```

## Build

```bash
pnpm build
```
